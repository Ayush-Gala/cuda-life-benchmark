Creating a visual display
    Frameworks? SDL seems good, but maybe overkill. Netbpm is a possible option, but maybe too much work to print simple images
        As of now, planning on using SDL
    display every frame to show movement?
    display just the fram of repetition? <-- This one is more inline with monitoring performance advancements

Evaluating oscilating board states
    use a constant max cycle number <-- straight forward and logical, but breaks badly if the cycle exceeds this value
    need a fixed board size in the event of a shape that infinitely generates in order to force some kind of 'steady' states

execution idea / game plan??
    user clicks on grid to choose initial cells
    choose run mode:
    → FAST: no delay, go nuts — mainly for benchmarking
    → SLOW: delay between frames so you can actually see what’s happening
    once done, show total runtime + final or “steady” state
    
    
cuda vars that I can use to flex with different GPUs:
    multiProcessorCount
    maxBlocksPerMultiProcessor
    maxThreadsPerBlock
        — using these = scale threads and blocks properly on whatever device it's running
    
    
note to self:
    be super careful with if statements inside warps
    → warp divergence kills perf
    → when threads in same warp take diff paths, they get serialized = bad
    so... avoid branching inside warps unless really needed
    

major bottleneck rn: initial HtoD mem transfer — slow bc using pageable mem
    → would be faster with pinned mem (duh)
    BUT the file that does the alloc is a .c file?? so I can't use cudaMallocHost()
    → annoying.
    → only fix I see: convert it to .cu, but that feels like overkill for one line
    
    
note for future me: why am I even avoiding .cu files??
    → mostly to keep CUDA parts isolated — makes it easier to read & find where kernels live
    → only want .cu files where actual GPU code happens
    ...might need to break that rule just this once so I can pin mem earlier
        
also hit stack overflow trying to test with huge data — probably just allocating too much when pinning. watch out for that.
    
TODO:
    figure out why mem copy fails if I launch kernel with too many blocks
    → ex: (1024, 1024) config + small grid (like 40x40) = it breaks
    → maybe it's overwriting something? maybe out of bounds?
    need to recreate & dig deeper